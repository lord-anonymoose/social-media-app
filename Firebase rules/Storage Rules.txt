rules_version = '2';
​
service firebase.storage {
  match /b/{bucket}/o {
    match /Posts/{postId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && postId.matches(request.auth.uid + '.*');
    }
    match /ProfilePictures/{pictureID} {
      allow read: if request.auth != null;
      allow write: if requested.auth != null && pictureID.matches(request.auth.uid + '.*');
    }
  }
}
